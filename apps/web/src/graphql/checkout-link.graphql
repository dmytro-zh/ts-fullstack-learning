query CheckoutLink($slug: String!) {
  checkoutLink(slug: $slug) {
    id
    slug
    active
    createdAt
    product {
      id
      name
      price
      inStock
      description
      imageUrl
    }
    store {
      id
      name
      email
    }
  }
}
