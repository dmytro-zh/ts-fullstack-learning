query StoresOverview {
  stores {
    id
    name
  }
}

query StoreDashboard($storeId: ID!) {
  products {
    id
    name
    price
    inStock
    storeId
    createdAt
    quantity
  }

  orders(storeId: $storeId) {
    id
    total
    createdAt
    status
    quantity
    product {
      id
      name
    }
  }
}
